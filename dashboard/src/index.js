import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Rendering main App component into root element
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

console.log("Index.js updated and running successfully!");
